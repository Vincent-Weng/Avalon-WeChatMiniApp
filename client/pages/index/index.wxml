<view class="index">
    <view class="nav">
        <navigator wx:for="{{labs}}" wx:key="{{lab.id}}" wx:for-item="lab" url="/pages/{{lab.id}}/{{lab.id}}">
            <text>{{lab.title}}</text>
        </navigator>
    </view>
    <view class='clear'>
      <button open-type="getUserInfo">授权用户信息</button>
    </view>
    <!-- <view class="clear">
        <button bindtap="clear">清空任务状态</button>
    </view> -->
    <!-- <view class='footnote'>
      <text text-align='center'>Developed by 翁瀚 Han, 卢村的豪哥</text>
    </view> -->
</view>